name: Anti-Pause Supabase

on:
  schedule:
    - cron: '0 9 * * 1,4' 
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keep-alive endpoint
        run: curl -f "https://your-url/keep-alive?password=yourpassword" || exit 1